# 🚀 Быстрый старт

## Шаг 1: Настройка Backend

```bash
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
python app.py
```

Сервер запустится на `http://localhost:5000`

## Шаг 2: Настройка Frontend (в новом терминале)

```bash
cd frontend
npm install
npm start
```

Приложение откроется на `http://localhost:3000`

## Готово! 🎉

- Откройте браузер: `http://localhost:3000`
- Админ-панель: `http://localhost:3000/admin` (пароль: `admin123`)

## Важные замечания

1. **Данные**: Файл `menu-database.json` уже скопирован в папку `data/`
2. **Пароль админа**: По умолчанию `admin123`, измените в `.env` файле
3. **Порты**: Backend на 5000, Frontend на 3000

## Структура проекта

```
Menu-New/
├── backend/          # Flask API
│   ├── app.py       # Главный файл сервера
│   └── requirements.txt
├── frontend/         # React приложение
│   └── src/
│       ├── pages/   # Страницы приложения
│       └── services/ # API сервисы
└── data/            # JSON данные
    └── menu-database.json
```

