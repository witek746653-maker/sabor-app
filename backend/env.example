#
# Пример переменных окружения для проекта (скопируйте в .env и заполните)
#
# Важно: файл .env содержит секреты. НЕ добавляйте .env в GitHub.
#

# Секретный ключ Flask (сгенерируйте случайную строку 32+ символа)
SECRET_KEY=change-me-generate-a-random-secret

# Путь к SQLite базе данных.
# Важно: если база лежит ВНУТРИ репозитория, её легко случайно "сбросить"
# (например, при чистой пересборке/копировании/деплое).
# Поэтому лучше указывать путь ВНЕ папки проекта.
#
# Примеры:
# Windows: SABOR_DB_PATH=D:\sabor-data\database.db
# Linux:   SABOR_DB_PATH=/var/lib/sabor-app/database.db
SABOR_DB_PATH=

# (Опционально) Автосоздание первого администратора при старте, если админа в БД нет.
# Это "страховка", если база была создана заново.
# ВАЖНО: это секреты — храните в .env и никогда не коммитьте.
BOOTSTRAP_ADMIN_NAME=Администратор
BOOTSTRAP_ADMIN_USERNAME=
BOOTSTRAP_ADMIN_PASSWORD=

# (Опционально) "Почти вечная" авторизация (в днях).
# Тех-термины:
# - "Сессия" (session) — cookie для входа
# - "Remember cookie" — cookie от Flask-Login, чтобы вход сохранялся даже после закрытия браузера
#
# Важно: совсем "никогда" нельзя гарантировать (пользователь может очистить cookies).
# Но можно поставить очень большой срок, например 3650 дней = 10 лет.
AUTH_SESSION_DAYS=3650
AUTH_REMEMBER_DAYS=3650

# Режим отладки (в продакшене False)
FLASK_DEBUG=False

# Порт для локального запуска (на VPS с nginx обычно не нужен)
PORT=5000

# Cookies:
# True  = только HTTPS (правильно для продакшена)
# False = временно можно тестировать по http://IP, пока нет домена/SSL
SESSION_COOKIE_SECURE=True

# CORS:
# Список адресов через запятую, которым разрешено обращаться к /api/*
# (нужно, если фронтенд открывается с другого домена/порта)
# Пример:
# CORS_ORIGINS=https://example.ru,https://www.example.ru,http://localhost:3000
CORS_ORIGINS=

