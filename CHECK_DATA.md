# 🔍 Проверка данных в базе - Вина и Барное меню

Если винная карта и барное меню не загружаются, скорее всего в базе данных нет данных.

---

## 🔍 Проверка данных в базе данных

### Шаг 1: Проверьте, есть ли данные

В консоли Bash на PythonAnywhere:

```bash
cd ~/sabor-app/backend
source venv/bin/activate
python
```

В Python выполните:

```python
from app import app, db
from models import Dish

with app.app_context():
    # Проверяем все блюда
    all_dishes = Dish.query.all()
    print(f'Всего блюд в базе: {len(all_dishes)}')
    
    # Проверяем вина
    wines = Dish.query.filter(Dish.menu == 'Вино').all()
    print(f'Вин в базе: {len(wines)}')
    
    # Проверяем барное меню
    bar_items = Dish.query.filter(Dish.menu == 'Барное меню').all()
    print(f'Барных напитков в базе: {len(bar_items)}')
    
    # Проверяем все уникальные меню
    from sqlalchemy import distinct
    menus = db.session.query(distinct(Dish.menu)).all()
    print(f'\nДоступные меню в базе:')
    for menu in menus:
        if menu[0]:
            count = Dish.query.filter(Dish.menu == menu[0]).count()
            print(f'  - {menu[0]}: {count} блюд')

exit()
```

---

## ✅ Если данных нет - загрузите их

### Вариант 1: Использовать миграцию (если есть JSON файл)

Если у вас есть файл `menu-database.json` с данными:

```bash
cd ~/sabor-app/backend
source venv/bin/activate
python migrate_to_db.py
```

Это загрузит все данные из JSON файла в базу данных.

---

### Вариант 2: Проверить JSON файл

Проверьте, есть ли файл с данными:

```bash
cd ~/sabor-app
ls -la data/menu-database.json
# или
ls -la frontend/public/data/menu-database.json
```

Если файл есть, используйте миграцию (Вариант 1).

---

### Вариант 3: Проверить, какие меню есть в JSON

Если нужно проверить, какие меню есть в JSON файле:

```bash
cd ~/sabor-app/backend
source venv/bin/activate
python
```

В Python:

```python
import json

# Попробуйте найти JSON файл
json_paths = [
    '../data/menu-database.json',
    '../frontend/public/data/menu-database.json',
    'data/menu-database.json'
]

for path in json_paths:
    try:
        with open(path, 'r', encoding='utf-8') as f:
            data = json.load(f)
            print(f'\nФайл найден: {path}')
            print(f'Всего блюд в JSON: {len(data)}')
            
            # Проверяем меню
            menus = {}
            for dish in data:
                menu = dish.get('menu', 'Unknown')
                menus[menu] = menus.get(menu, 0) + 1
            
            print('\nМеню в JSON файле:')
            for menu, count in menus.items():
                print(f'  - {menu}: {count} блюд')
            break
    except FileNotFoundError:
        continue

exit()
```

---

## 🔧 Если данные есть, но не загружаются

### Проверьте, правильное ли название меню

Вина ищутся по `menu = 'Вино'`, барное меню по `menu = 'Барное меню'`.

Если в JSON файле используются другие названия, нужно:
1. Проверить, какие названия используются в JSON
2. Либо изменить данные в базе
3. Либо изменить код в `backend/app.py`

---

## 📝 Быстрая проверка через API

Проверьте API напрямую в браузере:

1. **Вина:**
   ```
   https://romabornthisway.pythonanywhere.com/api/wines
   ```
   Должен вернуться JSON массив (даже если пустой `[]`).

2. **Барное меню:**
   ```
   https://romabornthisway.pythonanywhere.com/api/bar-items
   ```
   Должен вернуться JSON массив (даже если пустой `[]`).

Если возвращаются пустые массивы `[]` - данных нет в базе, нужно загрузить.

---

## ✅ Решение: Загрузить данные

Если данных нет, загрузите их:

1. Убедитесь, что есть файл `menu-database.json`
2. Запустите миграцию: `python migrate_to_db.py`
3. Проверьте результат через API

---

**После загрузки данных винная карта и барное меню должны работать!** 🎉
