# 🔑 Как создать Personal Access Token для GitHub

GitHub больше не принимает обычные пароли для Git операций через HTTPS. Вместо этого нужно использовать **Personal Access Token (PAT)**.

---

## 📝 Пошаговая инструкция

### Шаг 1: Откройте страницу создания токена

1. Зайдите на https://github.com
2. Войдите в свой аккаунт
3. Перейдите по ссылке: **https://github.com/settings/tokens**
   
   Или через меню:
   - Нажмите на вашу аватарку (правый верхний угол)
   - Выберите "Settings"
   - В левом меню выберите "Developer settings"
   - Выберите "Personal access tokens" → "Tokens (classic)"

### Шаг 2: Создайте новый токен

1. Нажмите кнопку **"Generate new token"**
2. Выберите **"Generate new token (classic)"**

### Шаг 3: Настройте токен

1. **Note (название токена):**
   - Введите понятное название, например: `Beget Deploy` или `GitHub Actions`
   - Это поможет вам понять, для чего этот токен

2. **Expiration (срок действия):**
   - Выберите срок действия:
     - `90 days` - токен будет работать 90 дней
     - `No expiration` - токен не будет истекать (удобнее, но менее безопасно)
   - Рекомендуется: `90 days` или `No expiration` для деплоя

3. **Select scopes (права доступа):**
   
   Минимально необходимые права:
   - ✅ **`repo`** - Full control of private repositories
     - Этот scope включает все права для работы с репозиториями
     - Нужен, если репозиторий приватный
   
   Если репозиторий публичный, можно использовать:
   - ✅ **`public_repo`** - Access public repositories
   
   **Рекомендация:** Используйте `repo`, это проще и работает для всех случаев.

### Шаг 4: Создайте токен

1. Прокрутите вниз
2. Нажмите кнопку **"Generate token"** (зеленая кнопка внизу)

### Шаг 5: Скопируйте токен

⚠️ **КРИТИЧЕСКИ ВАЖНО:**

- Токен показывается **ТОЛЬКО ОДИН РАЗ**!
- Как только вы закроете эту страницу, токен больше не будет виден
- **Скопируйте токен сразу!**

Токен выглядит примерно так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

Скопируйте весь токен (он начинается с `ghp_`).

### Шаг 6: Сохраните токен в безопасном месте

- Сохраните токен в безопасном месте (менеджер паролей, зашифрованный файл)
- Не публикуйте токен нигде!
- Если токен утечет, его можно отозвать (revoke) на странице токенов

---

## 🔐 Как использовать токен

### При клонировании репозитория

Когда система запросит:
```
Username for 'https://github.com': witek746653-maker
Password for 'https://witek746653-maker@github.com':
```

1. **Username:** введите ваш GitHub username (`witek746653-maker`)
2. **Password:** введите **Personal Access Token** (не обычный пароль!)

⚠️ **Важно:** При вводе пароля символы не отображаются - это нормально! Просто вставьте токен и нажмите Enter.

### В URL (не рекомендуется, но работает)

Можно также указать токен прямо в URL:

```bash
git clone https://YOUR_TOKEN@github.com/witek746653-maker/sabor-app.git
```

Но это менее безопасно, так как токен будет виден в истории команд.

---

## 🔒 Безопасность

1. **Никогда не коммитьте токен в Git!**
   - Токен не должен попадать в файлы репозитория
   - Если случайно закоммитили - сразу отзовите токен и создайте новый

2. **Используйте минимально необходимые права**
   - Не давайте токену больше прав, чем нужно

3. **Отзывайте неиспользуемые токены**
   - Если токен больше не нужен, отзовите его на странице настроек

4. **Не делитесь токенами**
   - Каждый должен иметь свой токен

---

## 🆘 Что делать, если токен утерян

Если вы забыли токен:

1. Зайдите на https://github.com/settings/tokens
2. Найдите ваш токен в списке (по названию, которое вы дали)
3. Если токен все еще активен - вы не можете его увидеть, но можете:
   - Отозвать (revoke) и создать новый
   - Или создать новый токен с другим названием

---

## 📚 Полезные ссылки

- Страница токенов: https://github.com/settings/tokens
- Документация GitHub: https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token

---

**Готово!** Теперь вы можете использовать этот токен вместо пароля при работе с Git через HTTPS.
