# 🚀 Развертывание приложения на Beget

Это подробная инструкция по развертыванию вашего приложения на российском хостинге Beget.

**Что такое развертывание (deploy)?**  
Развертывание - это процесс загрузки вашего приложения на сервер, чтобы оно стало доступным в интернете.

---

## 📋 Подготовка проекта

### 1. Соберите фронтенд (React)

Фронтенд нужно собрать (скомпилировать) перед загрузкой на сервер.

**В папке проекта выполните:**

```bash
cd frontend
npm install
npm run build
```

**Что делает `npm run build`?**  
Команда собирает ваш React проект в готовые файлы (HTML, CSS, JavaScript), которые может отдавать веб-сервер.  
Результат сборки появляется в папке `frontend/build/`.

**Важно:** После сборки проверьте, что папка `frontend/build/` существует и содержит файлы!

**Важно про Git:** папка `frontend/build/` добавлена в `.gitignore`, то есть **в GitHub её обычно нет**.  
Это значит:
- если вы деплоите на Beget через `git clone`, вам почти всегда нужно **собрать фронтенд на сервере** (если есть Node.js),  
  или **собрать локально и загрузить `frontend/build/` на сервер** (если Node.js на тарифе недоступен).

---

## 🔧 Настройка на Beget

### Шаг 1: Загрузите проект на сервер Beget

**✅ РЕКОМЕНДУЕТСЯ: Через Git (лучший вариант!)**

**Почему Git лучше?**
- 🚀 **Быстрое обновление**: Чтобы обновить код на сервере, просто выполните `git pull`
- 📦 **Автоматический бэкап**: Весь ваш код хранится в GitHub - если что-то случится с сервером, код не потеряется
- 🔄 **История изменений**: Видно, что и когда было изменено
- 👥 **Проще работать в команде**: Если захотите привлечь других разработчиков

1. В панели Beget откройте **"Файловый менеджер"** или подключитесь по **SSH**
2. Перейдите в папку `www` (или `public_html` - зависит от вашей настройки)
3. Клонируйте ваш репозиторий:

```bash
cd ~/www  # или cd ~/public_html - зависит от настроек Beget
git clone https://github.com/ваш-username/sabor-app.git
cd sabor-app
```

**⚠️ Важно:** При клонировании через SSH может потребоваться GitHub Personal Access Token (см. `GITHUB_TOKEN.md`)

**Альтернативный способ: Через FTP (если Git недоступен)**

1. Используйте FTP-клиент (FileZilla, WinSCP и т.д.)
2. Загрузите все файлы проекта в папку `www` или `public_html`

**⚠️ Недостаток FTP:** При каждом обновлении нужно загружать файлы заново вручную.

---

### Шаг 2: Создайте виртуальное окружение Python

**Виртуальное окружение** - это изолированная среда для установки библиотек Python вашего проекта.

**В консоли Beget (SSH или через панель) выполните:**

```bash
cd ~/www/sabor-app  # или путь к вашему проекту
python3 -m venv venv
source venv/bin/activate  # Активируем виртуальное окружение
```

**Проверка:** После активации в начале строки должно появиться `(venv)`.

---

### Шаг 3: Установите зависимости Python

**В активированном виртуальном окружении выполните:**

```bash
cd backend
pip install --upgrade pip
pip install -r requirements.txt
```

**Проверка:** Все библиотеки должны установиться без ошибок.

---

### Шаг 4: Настройте переменные окружения

Создайте файл `.env` в папке `backend/`:

```bash
cd ~/www/sabor-app/backend
nano .env  # или используйте другой редактор
```

**Содержимое файла `.env`:**

```env
# Секретный ключ для Flask (придумайте сложный случайный ключ!)
SECRET_KEY=ваш-очень-сложный-секретный-ключ-минимум-32-символа

# Режим отладки (False для продакшена)
FLASK_DEBUG=False

# Порт (обычно на Beget не нужен, но можно указать)
PORT=5000
```

**Как придумать SECRET_KEY?**  
Можно использовать генератор паролей или сгенерировать случайную строку:
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

**Важно:** Никогда не делитесь файлом `.env`! В нём хранятся секретные данные.

---

### Шаг 5: Создайте базу данных

В этом проекте база данных **SQLite** — это файл `backend/database.db`.

**Хорошая новость:** таблицы создаются **автоматически** при первом запуске приложения (в конце `backend/app.py` есть `db.create_all()`).

#### Вариант A (самый простой): просто запустите приложение один раз

После первого старта на сервере появится файл `backend/database.db` (если у процесса есть права на запись).

#### Вариант B (если нужно заполнить базу данными из JSON)

Это называется **миграция** (migration) — перенос данных из одного формата в другой (из JSON в SQLite).

На сервере выполните:

```bash
cd ~/www/sabor-app/backend
source ../venv/bin/activate

# Переносим данные из data/menu-database.json в backend/database.db
python3 migrate_to_db.py
```

**Проверка:** должен появиться файл `backend/database.db`, а скрипт напишет, сколько блюд загрузил.

---

### Шаг 6: Создайте администратора

**Создайте администратора для входа в админ-панель:**

```bash
cd ~/www/sabor-app/backend
source ../venv/bin/activate
python3 create_admin.py
```

Следуйте инструкциям скрипта - введите имя, логин и пароль администратора.

---

### Шаг 7: Соберите фронтенд на сервере (если не собрали локально)

**Если вы не собрали фронтенд локально, сделайте это на сервере:**

```bash
cd ~/www/sabor-app/frontend

# Установите Node.js зависимости (если Node.js доступен на Beget)
npm install

# Соберите проект
npm run build
```

**Примечание:** На некоторых тарифах Beget Node.js может быть недоступен.  
В этом случае собирайте фронтенд локально и загружайте папку `build/` через FTP.

---

### Шаг 8: Настройте Python-приложение в панели Beget

1. **Войдите в панель управления Beget:** https://cp.beget.com
2. Перейдите в раздел **"Сайты"** → **"Управление сайтами"**
3. Найдите ваш сайт или создайте новый
4. Откройте **"Настройки Python"** или **"Python-приложения"**
5. **Настройте приложение:**

   - **Виртуальное окружение:** `~/www/sabor-app/venv` (или полный путь)
   - **WSGI файл:** `~/www/sabor-app/wsgi.py` (полный путь к файлу)
   - **Рабочая директория:** `~/www/sabor-app` (корень проекта)

6. **Доменное имя:** Укажите ваш домен (например, `yourdomain.ru`)

**Важно:** файл `wsgi.py` уже настроен так, чтобы сам определить корень проекта через `Path(__file__)`.  
Обычно **ничего править не нужно** — достаточно указать правильные пути в панели Beget (WSGI файл, рабочая директория и venv).

---

### Шаг 9: Настройте права доступа к файлам

**В консоли Beget выполните:**

```bash
cd ~/www/sabor-app
chmod 755 wsgi.py
chmod 644 backend/database.db  # Если файл уже создан (если нет — пропустите)
chmod 755 backend/
chmod 755 frontend/build/  # Если папка существует
```

**Что такое chmod?**  
`chmod` - это команда для изменения прав доступа к файлам.  
- `755` - владелец может читать/писать/выполнять, остальные - читать/выполнять
- `644` - владелец может читать/писать, остальные - только читать

---

### Шаг 10: Перезапустите приложение

В панели Beget найдите ваш Python-приложение и нажмите **"Перезапустить"** или **"Restart"**.

---

## ✅ Проверка работы

### 1. Проверьте API (бэкенд)

Откройте в браузере:

```
https://yourdomain.ru/api/dishes
```

**Должен вернуться JSON** с данными о блюдах (может быть пустой массив `[]`, если база пустая).

### 2. Проверьте фронтенд

Откройте в браузере:

```
https://yourdomain.ru
```

**Должен открыться ваш сайт!**

### 3. Проверьте админ-панель

Откройте:

```
https://yourdomain.ru/admin/login
```

**Войдите** с логином и паролем администратора, которого вы создали.

---

## 🐛 Решение проблем

### Проблема: Ошибка 500 (Internal Server Error)

**Что делать:**

1. **Проверьте логи ошибок в панели Beget:**
   - Раздел **"Логи"** → **"Ошибки Python"** или **"Error log"**

2. **Проверьте, правильно ли указан путь в `wsgi.py`:**
   - Откройте файл `wsgi.py`
   - Убедитесь, что путь `PROJECT_ROOT` соответствует реальному пути на сервере

3. **Проверьте виртуальное окружение:**
   ```bash
   source ~/www/sabor-app/venv/bin/activate
   python3 -c "from backend.app import app; print('OK')"
   ```
   Если ошибка - проверьте установку зависимостей.

### Проблема: База данных не создаётся

**Что делать:**

1. **Проверьте права на запись:**
   ```bash
   chmod 755 ~/www/sabor-app/backend
   ```

2. **Создайте базу вручную:**
   ```bash
   cd ~/www/sabor-app/backend
   source ../venv/bin/activate
   python3
   ```
   ```python
   from app import app, db
   with app.app_context():
       db.create_all()
       exit()
   ```

### Проблема: Фронтенд не открывается (белая страница)

**Что делать:**

1. **Проверьте, что папка `frontend/build/` существует:**
   ```bash
   ls -la ~/www/sabor-app/frontend/build/
   ```

2. **Если папки нет - соберите фронтенд:**
   ```bash
   cd ~/www/sabor-app/frontend
   npm run build
   ```

3. **Проверьте права доступа:**
   ```bash
   chmod -R 755 ~/www/sabor-app/frontend/build/
   ```

### Проблема: "ModuleNotFoundError" при запуске

**Что делать:**

1. **Активируйте виртуальное окружение в `wsgi.py`:**
   - Откройте `wsgi.py`
   - Убедитесь, что путь к `venv` правильный

2. **Переустановите зависимости:**
   ```bash
   source ~/www/sabor-app/venv/bin/activate
   cd ~/www/sabor-app/backend
   pip install -r requirements.txt
   ```

### Проблема: Cookies не работают (не могу войти)

**Что делать:**

1. **Проверьте, что используется HTTPS:**
   - Beget обычно предоставляет SSL-сертификат
   - Убедитесь, что `SESSION_COOKIE_SECURE = True` в `backend/app.py`
   - Если вы **ещё не включили HTTPS**, временно поставьте `SESSION_COOKIE_SECURE = False`, иначе браузер не будет сохранять cookie

2. **Проверьте настройки CORS:**
   - В `app.py` должно быть `supports_credentials=True` в настройках CORS

---

## 📝 Обновление приложения

**Когда вы обновили код, чтобы применить изменения:**

### Вариант 1: Если используете Git (рекомендуется)

**На вашем компьютере (локально):**

1. Внесите изменения в код
2. Сохраните изменения в GitHub:

```bash
git add .
git commit -m "Описание изменений"
git push
```

**На сервере Beget (через SSH или консоль):**

1. Подключитесь к серверу Beget
2. Обновите код с GitHub:

```bash
cd ~/www/sabor-app  # или ваш путь к проекту
git pull
```

**Вот и всё!** Код обновится автоматически.

### Вариант 2: Если используете FTP

**Придётся каждый раз:**
1. Скачать изменённые файлы
2. Загрузить их через FTP-клиент на сервер
3. Это медленнее и неудобнее

**Вывод:** Используйте Git! Это намного удобнее! 🚀

### 2. Если изменились зависимости Python:

```bash
source venv/bin/activate
cd backend
pip install -r requirements.txt
```

### 3. Если изменился фронтенд:

```bash
cd frontend
npm run build
```

### 4. Перезапустите приложение в панели Beget

---

## 🔐 Безопасность

### Важные рекомендации:

1. **SECRET_KEY:** Обязательно установите сложный SECRET_KEY в `.env`
2. **FLASK_DEBUG:** Всегда устанавливайте `FLASK_DEBUG=False` на продакшене
3. **`.env` файл:** Никогда не коммитьте `.env` в Git! Добавьте его в `.gitignore`
4. **Пароли:** Используйте сложные пароли для администраторов
5. **Права доступа:** Не давайте слишком широкие права к файлам (например, `777`)

---

## 📞 Контакты и поддержка

**Если возникли проблемы:**

1. Проверьте логи ошибок в панели Beget
2. Посмотрите раздел "Решение проблем" выше
3. Обратитесь в поддержку Beget: https://beget.com/ru/support

---

## ✅ Чеклист развертывания

- [ ] Фронтенд собран (`frontend/build/` существует)
- [ ] Проект загружен на сервер Beget
- [ ] Создано виртуальное окружение Python
- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Создан файл `.env` с SECRET_KEY
- [ ] Создана база данных (`database.db`)
- [ ] Создан администратор (`create_admin.py`)
- [ ] Настроено Python-приложение в панели Beget
- [ ] Указан правильный путь к `wsgi.py`
- [ ] Установлены правильные права доступа (`chmod`)
- [ ] Приложение перезапущено в панели Beget
- [ ] Проверена работа API (`/api/dishes`)
- [ ] Проверена работа фронтенда (главная страница)
- [ ] Проверена работа админ-панели (`/admin/login`)

**Готово! Ваше приложение должно работать! 🎉**
