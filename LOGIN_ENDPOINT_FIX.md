# 🔴 Исправление ошибки "Could not build url for endpoint 'login'"

Ошибка в логах:
```
BuildError: Could not build url for endpoint 'login'. Did you mean 'admin_login' instead?
```

## 🔍 Причина проблемы

Flask-Login настроен на endpoint `'login'`, но в коде endpoint называется `'admin_login'`. Нужно исправить настройку.

---

## ✅ Решение

### Шаг 1: Откройте файл `backend/app.py` на вашем компьютере

Откройте файл `d:\GitHub\sabor-app\backend\app.py` в редакторе.

### Шаг 2: Найдите строку с `login_view`

Найдите строку (около строки 37):
```python
login_manager.login_view = 'login'
```

### Шаг 3: Измените на 'admin_login'

Замените на:
```python
login_manager.login_view = 'admin_login'
```

Или если у вас уже есть маршрут с именем функции, проверьте имя функции для `/api/admin/login`.

### Шаг 4: Проверьте имя функции

Убедитесь, что функция для `/api/admin/login` называется `admin_login`:

```python
@app.route('/api/admin/login', methods=['POST'])
def admin_login():  # <-- имя функции
    ...
```

Имя функции должно совпадать с `login_view`.

### Шаг 5: Закоммитьте и загрузите на PythonAnywhere

1. Сохраните файл
2. Закоммитьте изменения:
```bash
cd d:\GitHub\sabor-app
git add backend/app.py
git commit -m "Fix login_view endpoint name"
git push
```

3. На PythonAnywhere обновите код:
```bash
cd ~/sabor-app
git pull
```

4. Перезагрузите приложение:
   - PythonAnywhere Dashboard → Web → Reload

---

## 🔧 Альтернативное решение (если не хотите менять код)

Можно добавить алиас для endpoint, но проще исправить `login_view`.

---

## ✅ После исправления

После того, как исправите и перезагрузите приложение:
1. Ошибка должна исчезнуть
2. Админ-панель должна работать корректно

---

**Исправление простое - нужно всего лишь изменить одну строку!** 🎉
