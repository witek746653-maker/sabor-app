# 🚀 Инструкция по деплою проекта

Этот документ содержит пошаговые инструкции по развертыванию вашего приложения на Netlify (фронтенд) и PythonAnywhere (бэкенд).

---

## 📋 Содержание

1. [Подключение к GitHub](#1-подключение-к-github)
2. [Настройка Netlify (фронтенд)](#2-настройка-netlify-фронтенд)
3. [Настройка PythonAnywhere (бэкенд)](#3-настройка-pythonanywhere-бэкенд)
4. [Проверка работы](#4-проверка-работы)

---

## 1. Подключение к GitHub

### Шаг 1.1: Изменение Git remote

Текущий remote указывает на старый репозиторий. Нужно изменить его на `sabor-app`.

**ВНИМАНИЕ:** Замените `YOUR_GITHUB_USERNAME` на ваш реальный GitHub username!

Выполните в терминале (в папке проекта `d:\GitHub\sabor-app`):

```bash
git remote set-url origin https://github.com/YOUR_GITHUB_USERNAME/sabor-app.git
```

Проверить, что remote изменился:
```bash
git remote -v
```

Должно показать:
```
origin  https://github.com/YOUR_GITHUB_USERNAME/sabor-app.git (fetch)
origin  https://github.com/YOUR_GITHUB_USERNAME/sabor-app.git (push)
```

### Шаг 1.2: Загрузка кода в GitHub

Если репозиторий уже создан на GitHub, выполните:

```bash
git push -u origin main
```

Если репозиторий пустой или вы первый раз пушите, возможно нужно сначала создать репозиторий на GitHub:
1. Зайдите на https://github.com
2. Нажмите "New repository"
3. Назовите его `sabor-app`
4. НЕ добавляйте README, .gitignore или лицензию (они уже есть)
5. Создайте репозиторий
6. Затем выполните команду `git push -u origin main` выше

---

## 2. Настройка Netlify (фронтенд)

### Шаг 2.1: Создание аккаунта и подключение репозитория

1. Зайдите на https://www.netlify.com
2. Зарегистрируйтесь или войдите (можно через GitHub)
3. Нажмите "Add new site" → "Import an existing project"
4. Выберите "GitHub" и разрешите доступ
5. Найдите репозиторий `sabor-app` и выберите его

### Шаг 2.2: Настройка параметров сборки

Netlify автоматически обнаружит файл `netlify.toml`, но нужно указать:

**Build settings:**
- **Base directory:** `frontend`
- **Build command:** `npm ci && npm run build` (или `npm install && npm run build`)
- **Publish directory:** `frontend/build`

**Environment variables (Environment variables → Add variable):**
- **Key:** `REACT_APP_API_URL`
- **Value:** URL вашего бэкенда на PythonAnywhere (например: `https://yourusername.pythonanywhere.com`)

⚠️ **ВАЖНО:** URL должен быть полным, с `https://`, и БЕЗ слеша в конце!

Например:
```
REACT_APP_API_URL=https://yourusername.pythonanywhere.com
```

### Шаг 2.3: Деплой

1. Нажмите "Deploy site"
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. После успешной сборки сайт будет доступен по адресу типа: `https://random-name-123.netlify.app`

### Шаг 2.4: Настройка доменного имени (опционально)

1. В настройках сайта перейдите в "Domain settings"
2. Можете изменить автоматическое имя на что-то понятное
3. Или подключите свой домен

---

## 3. Настройка PythonAnywhere (бэкенд)

### Шаг 3.1: Создание аккаунта

1. Зайдите на https://www.pythonanywhere.com
2. Зарегистрируйтесь (есть бесплатный план)
3. После регистрации вы попадете в веб-консоль (Dashboard)

### Шаг 3.2: Загрузка файлов

#### Вариант А: Через Git (рекомендуется)

1. Откройте вкладку "Consoles" → "Bash"
2. Выполните:
```bash
cd ~
git clone https://github.com/YOUR_GITHUB_USERNAME/sabor-app.git
cd sabor-app/backend
```

#### Вариант Б: Через веб-интерфейс

1. Откройте вкладку "Files"
2. Создайте папку `sabor-app`
3. Загрузите файлы из папки `backend` через веб-интерфейс

### Шаг 3.3: Настройка виртуального окружения

В консоли Bash (Consoles → Bash):

```bash
cd ~/sabor-app/backend
python3.10 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 3.4: Создание файла .env

**Для локальной разработки (на вашем компьютере):**

Создайте файл `backend/.env` (скопируйте содержимое ниже):

```
SECRET_KEY=ваш-секретный-ключ-сгенерируйте-случайную-строку
PORT=5000
FLASK_DEBUG=True
```

**Для PythonAnywhere (в консоли):**

```bash
cd ~/sabor-app/backend
nano .env
```

Скопируйте и вставьте (нажмите Ctrl+Shift+V для вставки в nano):

```
SECRET_KEY=ваш-секретный-ключ-сгенерируйте-случайную-строку
PORT=5000
FLASK_DEBUG=False
```

**Как сгенерировать SECRET_KEY:**
В консоли выполните:
```bash
python3.10 -c "import secrets; print(secrets.token_hex(32))"
```
Скопируйте вывод и используйте как SECRET_KEY.

В nano:
- Нажмите Ctrl+O для сохранения
- Нажмите Enter для подтверждения
- Нажмите Ctrl+X для выхода

### Шаг 3.5: Инициализация базы данных

В консоли:

```bash
cd ~/sabor-app/backend
source venv/bin/activate
python migrate_to_db.py
```

(Если у вас есть данные для миграции)

### Шаг 3.6: Создание WSGI файла

1. Откройте вкладку "Web"
2. Нажмите "Add a new web app"
3. Выберите "Manual configuration"
4. Выберите Python 3.10
5. Нажмите "Next" → "Next"

6. В разделе "Code" найдите путь к WSGI файлу (например: `/var/www/yourusername_pythonanywhere_com_wsgi.py`)
7. Нажмите на ссылку файла, чтобы открыть его для редактирования
8. Замените всё содержимое на:

```python
import sys

# Добавляем путь к проекту
path = '/home/yourusername/sabor-app/backend'
if path not in sys.path:
    sys.path.insert(0, path)

# Активируем виртуальное окружение
activate_this = '/home/yourusername/sabor-app/backend/venv/bin/activate_this.py'
with open(activate_this) as file_:
    exec(file_.read(), dict(__file__=activate_this))

# Импортируем приложение
from app import app as application

if __name__ == "__main__":
    application.run()
```

⚠️ **ВАЖНО:** Замените `yourusername` на ваш реальный PythonAnywhere username!

**Как узнать свой username:**
- В верхней части страницы PythonAnywhere написано: "Logged in as yourusername"
- Или в консоли выполните: `whoami`

### Шаг 3.7: Настройка путей к файлам

Если ваш бэкенд использует файлы (изображения, аудио), нужно настроить пути:

1. Во вкладке "Web" найдите раздел "Static files"
2. Добавьте mapping:
   - **URL:** `/images/`
   - **Directory:** `/home/yourusername/sabor-app/images/`

3. Если есть аудио:
   - **URL:** `/audio/`
   - **Directory:** `/home/yourusername/sabor-app/audio/`

⚠️ **ВАЖНО:** Замените `yourusername` на ваш реальный username!

### Шаг 3.8: Настройка CORS (если нужно)

В файле `backend/app.py` уже настроен CORS, но убедитесь, что в настройках PythonAnywhere разрешены запросы с вашего домена Netlify.

### Шаг 3.9: Запуск приложения

1. Во вкладке "Web" нажмите зеленую кнопку "Reload"
2. Подождите несколько секунд
3. Ваш бэкенд будет доступен по адресу: `https://yourusername.pythonanywhere.com`

---

## 4. Проверка работы

### 4.1: Проверка бэкенда

Откройте в браузере:
```
https://yourusername.pythonanywhere.com/api/dishes
```

Должен вернуться JSON с блюдами (или пустой массив, если данных нет).

### 4.2: Проверка фронтенда

1. Откройте ваш сайт на Netlify
2. Проверьте, что страницы загружаются
3. Попробуйте выполнить какое-то действие (например, открыть меню)
4. Откройте консоль разработчика (F12) и проверьте, нет ли ошибок

### 4.3: Проверка связи фронтенда и бэкенда

1. В консоли браузера (F12) перейдите на вкладку "Network"
2. Обновите страницу
3. Должны быть видны запросы к API (к адресу PythonAnywhere)
4. Если видите ошибки CORS, проверьте настройки CORS в бэкенде

---

## 🔧 Решение проблем

### Проблема: Netlify не может собрать проект

**Решение:**
- Проверьте, что в `package.json` есть все зависимости
- Проверьте, что команда `npm run build` работает локально
- Посмотрите логи сборки в Netlify (вкладка "Deploys" → выберите деплой → "Deploy log")

### Проблема: Бэкенд на PythonAnywhere не запускается

**Решение:**
- Проверьте логи ошибок в PythonAnywhere (вкладка "Web" → "Error log")
- Убедитесь, что виртуальное окружение активировано в WSGI файле
- Проверьте, что все пути правильные (заменили `yourusername`)

### Проблема: CORS ошибки

**Решение:**
- В `backend/app.py` проверьте настройки CORS
- Убедитесь, что в `CORS(app, origins=[...])` указан домен Netlify
- Или используйте `origins="*"` для тестирования (небезопасно для продакшена!)

### Проблема: Переменные окружения не работают

**Решение:**
- В Netlify: убедитесь, что переменная `REACT_APP_API_URL` добавлена в настройках сайта
- В PythonAnywhere: убедитесь, что файл `.env` находится в папке `backend/`
- Перезапустите приложение после изменения переменных окружения

---

## 📝 Дополнительные замечания

1. **Бесплатный план PythonAnywhere:**
   - Приложение "засыпает" после неактивности
   - Первый запуск после "засыпания" может быть медленным
   - Можно настроить задачу (scheduled task) для "пробуждения" приложения

2. **Безопасность:**
   - Никогда не коммитьте файлы `.env` в Git!
   - Используйте сильные пароли для админ-панели
   - В продакшене настройте CORS только для вашего домена Netlify

3. **Обновление:**
   - После изменений в коде: `git push` → Netlify автоматически пересоберет
   - На PythonAnywhere: `git pull` в консоли → Reload в Web интерфейсе

---

## ✅ Чек-лист готовности

- [ ] Git remote настроен на правильный репозиторий
- [ ] Код загружен в GitHub
- [ ] Netlify подключен к репозиторию
- [ ] В Netlify указана переменная `REACT_APP_API_URL`
- [ ] Фронтенд успешно собран на Netlify
- [ ] PythonAnywhere аккаунт создан
- [ ] Код загружен на PythonAnywhere
- [ ] Виртуальное окружение создано и зависимости установлены
- [ ] Файл `.env` создан с правильными значениями
- [ ] WSGI файл настроен правильно
- [ ] База данных инициализирована
- [ ] Бэкенд запущен и доступен
- [ ] Фронтенд и бэкенд работают вместе

---

Удачи с деплоем! 🚀
